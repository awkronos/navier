# navier main-results manifest (staging artifact; repo copy at 6e9c2ec)
# Modeled on upstream openai/NavierStokesAndEuler formalization.yaml concept.
# Statuses and wording copied from /tmp/navier-crown/docs/OPEN_FRONTIER_MAP.md
# as of 2026-09-13. `evidence: unknown` means unmeasured, never a verdict.
version: 1
generated: "2026-09-13"
source_commit: "6e9c2ec"
toolchain: "leanprover/lean4:v4.34.0-rc2"
rows:
  - id: "crown_C_whole_space_forced"
    declaration: "Navier.Breakdown.ConstructedBreakdown.wholeSpaceBreakdown"
    file: "Navier/Breakdown/ConstructedBreakdown.lean"
    status: "THEOREM"
    axioms: ["propext", "Classical.choice", "Quot.sound"]
    evidence: "raw #print axioms 2026-09-13, Lean v4.34.0-rc2"
    statement: "Forced whole-space alternative C: the constructed whole-space breakdown holds; C itself has no remaining premise."
  - id: "crown_C_whole_space_successive_partials"
    declaration: "Navier.Breakdown.ConstructedBreakdown.wholeSpaceBreakdown_with_successivePartials"
    file: "Navier/Breakdown/ConstructedBreakdown.lean"
    status: "THEOREM"
    axioms: ["propext", "Classical.choice", "Quot.sound"]
    evidence: "raw #print axioms 2026-09-13, Lean v4.34.0-rc2"
    statement: "Forced whole-space alternative C strengthened with successive-partial regularity of the selected velocity before its deadline."
  - id: "crown_C_deadline_parameterized"
    declaration: "Navier.Breakdown.DeadlineParameterizedWholeSpaceBreakdown.wholeSpaceBreakdown_deadlineT"
    file: "Navier/Breakdown/DeadlineParameterizedWholeSpaceBreakdown.lean"
    status: "THEOREM"
    axioms: ["propext", "Classical.choice", "Quot.sound"]
    evidence: "raw #print axioms 2026-09-13, Lean v4.34.0-rc2"
    statement: "Deadline-parameterized forced alternative C: proved for every ν > 0 and every prescribed T > 0, force-support endpoint T₀·T with parallel rescaled velocity and native force carriers."
  - id: "crown_D_periodic_forced"
    declaration: "Navier.Analysis.PeriodicConstructedBreakdown.periodicBreakdown"
    file: "Navier/Analysis/PeriodicConstructedBreakdown.lean"
    status: "THEOREM"
    axioms: ["propext", "Classical.choice", "Quot.sound"]
    evidence: "raw #print axioms 2026-09-13, Lean v4.34.0-rc2"
    statement: "Periodic breakdown D: the native periodic forced endpoint has no remaining premise; it does not settle unforced periodic evolution."
  - id: "open_A_whole_space_unforced"
    declaration: "Navier.ProblemStatements.WholeSpaceGlobalRegularity"
    file: "Navier/Problem.lean"
    status: "OPEN"
    evidence: "docs/OPEN_FRONTIER_MAP.md 2026-09-13 row 'Unforced whole-space alternative A'"
    statement: "Unforced whole-space alternative A: local existence, normalized continuation and arbitrary-large-data critical control must meet in one faithful whole-space consumer."
  - id: "open_B_periodic_unforced"
    declaration: "Navier.ProblemStatements.PeriodicGlobalRegularity"
    file: "Navier/OfficialProblem.lean"
    status: "OPEN"
    evidence: "docs/OPEN_FRONTIER_MAP.md 2026-09-13 row 'Unforced periodic alternative B' (OPEN arbitrary data)"
    statement: "Unforced periodic alternative B, arbitrary data: small-data case settled on the lattice carrier (CriticalMildSmallDataGlobal.smallDataGlobalMild, ε(ν) = ν/16, 2026-09-11); arbitrary-data continuation OPEN."
  - id: "open_bkm_vorticity_integral_divergence_residual"
    declaration: "Navier.Analysis.BKMVorticityIntegralDivergence.lintegral_vorticity_integral_divergence"
    file: "Navier/Analysis/BKMVorticityIntegralDivergence.lean"
    status: "OPEN"
    evidence: "DECOMPOSED 2026-09-13 (lane L6b), refined 2026-09-14 (lanes L6b2, L6b3): BKMProfileGronwallPair single-file compile exit 0 zero warnings, six strict receipts (pair ≡ velocity estimate (dagger); constant-pair shortcut kernel-FALSIFIED via no_uniform_sup_bound consuming speed_unbounded; Biot–Savart bridge velocity_le_V). BKMProfileRateBound single-file compile exit 0, fourteen strict public declarations: the leaf is implied by EITHER of two quantitative envelopes for V u — (1) two-sided power-law sandwich c1(1-t)^{-alpha} <= V u t <= c2(1-t)^{-alpha} on [t0,1) with alpha > 1 (exact, consumed via the convergent antiderivative; upper envelope alone NEVER suffices — jump-then-envelope counterexamples), or (2) monotone + self-referential lag V u t <= A(1 + V u (lambda t))^q on geometric windows (the shape the staged cascade gains naturally produce); each envelope is wired through the L6b2 consumers to GronwallPair and both crown divergence corollaries. One-search map: no quantitative envelope for V u of the constructed field exists in-repo — only qualitative cascade hypotheses; the envelope is construction work, not a scalar argument; extended 2026-09-14 (lane L6b5): BKMProfileEnvelope single-file compile exit 0 zero warnings, seven strict receipts. The selected profile is now explicitly identified — the time-activated compactly-supported velocity of the R3CompactCandidate competitor (stage 0 = modulated slow base + initialization, stages j>=1 = positive cascade corrections) — and the envelope formats are welded to the coordinate the repo's quantitative layer actually emits through a NEW coordinate comparison 1-t <= physicalQ h (t,y) everywhere with equality on the axis (forwardScalar_le_self via coordinateQ_spec): vorticityRateBound_of_q_curlBound turns a spatially-uniform c2*physicalQ^(-alpha) pointwise curl upper bound plus the same-power axis lower bound into the leaf via the sandwich bridge, and the same hypothesis set feeds GronwallPair and both crown divergence corollaries. The axis exponent is in range: A h + 1/2 = 1 + h > 1. Surviving work is five named transports (per-stage ThreeCutBounds/RawStageBounds schedule-summation, stage-0 ActualPhysicalStageBounds, curl-to-vorticity transport, cut+activation sup reduction, axis correction domination), ; axis half supplied 2026-09-14 (lane L6b4): Navier.Construction.BaseVorticityAxis (7 strict declarations, zero warnings) computes the actual Euclidean curl of the Borel base on the symmetry axis EXACTLY — axial, value 2*C⁻¹*d.phi 0 (0,0)*(1-t)^(-(A h + 1/2)), transverse components vanish by the axis Jacobian; inv_phi_axis cancels the modulation to 2*v.profiles.f (0,0), f_axis_positive proves the constant > 0 through seedF_positive, and axis_vorticity_tendsto gives Tendsto at 1⁻ atTop with the sharp rate; exponent A h + 1/2 = 1 + h > 1 matches the bridge's alpha window. The surviving axis-side transport is domination of the full activated field's correction jets by this base term at the axis; hup transports 1-4 remain — no crown declaration unconditional"
    statement: "Residual: the scalar vorticity-rate estimate for the selected profile of the CONSTRUCTED FORCED field (not unforced NS): VorticityRateBound u := exists C2, 0 <= C2 and forall t in Ico 0 1, V u t <= C2 * exp(integral 0..t V u s). Everything from this leaf to the crown is proved (L6b2: pair iff (dagger); rate bound gives pair and divergence corollaries). L6b3 reduces the leaf itself to constructing EITHER envelope (1) or (2) above for V u from the construction data — strict conditional bridges vorticityRateBound_of_sandwich_for / vorticityRateBound_of_monotoneLag_for discharge it; neither envelope has a producer in the estate"
  - id: "conditional_mild_fixed_point_assembly"
    declaration: "Navier.Analysis.ContinuousLeiLinMildFixedPoint.actual_existsUnique_mildFixedPoint"
    file: "Navier/Analysis/ContinuousLeiLinMildFixedPoint.lean"
    status: "CONDITIONAL_THEOREM"
    evidence: "raw #print axioms 2026-09-13 at this head: '...actual_existsUnique_mildFixedPoint' depends on axioms: [propext, Classical.choice, Quot.sound]; selfMapEstimate, mildLift_mem and mildLift_contraction likewise strict"
    statement: "Existence and uniqueness of the fixed point of the actual mild self-map on the completed linked box ActualLinkedBox ν T (2R) (2R), derived contraction factor 3/4 under the consumed threshold R ≤ ν/16. The travelling premise is the MildAssemblyLeaves record; Φ x = x is carrier equality, not the pointwise PDE. Premise status 2026-09-13: the record is decomposed (joint-leaves row closed; the time/polarization row OPEN — five of six time fields now closed at every horizon time (2a/2b/2d/2f), the X¹ leaf 2c closed a.e. in time, see the instantiation row), its pointwise fields are restated over t ∈ Icc 0 T — the former ∀t domain obstruction at t < 0 (heat-multiplier explosion, structural witness a = exp(−‖ξ‖²) at ν = 1, t = −1) is REPAIRED in the record and its consumers via the zero extension mildImageIcc — AND the premise is WITNESSED INHABITED (zero-box row) — so the conditional is non-vacuous; what remains open is its non-degenerate discharge, a live construction target, not an assumption of the crown."
  - id: "closed_mild_assembly_joint_leaves"
    declaration: "Navier.Analysis.ContinuousLeiLinMildAssemblyLeaves.mildAssemblyLeaves_of_namedLeaves"
    file: "Navier/Analysis/ContinuousLeiLinMildAssemblyLeaves.lean"
    status: "THEOREM"
    axioms: ["propext", "Classical.choice", "Quot.sound"]
    evidence: "raw #print axioms 2026-09-13, lane-rerun single-file compile exit 0 zero warnings plus locked audit build: all 22 module declarations incl. the joint-measurability primitive exists_stronglyMeasurable_jointVersion, the shared convolution lemma, the three record-shaped leaves mildLeafHjointDiag/L/R, the five S3 time-leaf theorems mildTimeLeaf_hmM/hmXm1/d3X1/hmX1_ae/hmX1Int and the split/projection trio mildAssemblyLeaves_of_namedLeaves plus both forward projections print exactly [propext, Classical.choice, Quot.sound]; ConditionalAudit emits the joint-proxy, diagonal-leaf, assembly and time-leaf receipts on every lake build"
    statement: "The joint (ξ,t) a.e. strong measurability leaves of MildAssemblyLeaves — diagonal and both ordered mixed Navier sources — are constructed for GENERAL (quotient-represented) box elements, from one shared joint-version primitive (an L¹_t(L¹_ξ) slot element admits a jointly strongly measurable version whose sections represent a.e.) transported through the genuine Leray source via convolution congruence. The record is shown equivalent (both directions: of_namedLeaves and the two forward projections) to the three closed leaves plus hRν plus MildAssemblyTimeLeaves and MildAssemblyPolarizationLeaves."
  - id: "open_mild_assembly_leaves_instantiation"
    declaration: "Navier.Analysis.ContinuousLeiLinMildAssemblyLeaves.MildAssemblyTimeLeaves"
    file: "Navier/Analysis/ContinuousLeiLinMildAssemblyLeaves.lean"
    status: "OPEN"
    evidence: "DECOMPOSED 2026-09-13 (lane L6a joint leaves; lane L6c time-leaf restatement + partial closure): the domain obstruction is REPAIRED in the record — fields hmM/hmXm1/hmX1 are restated over t ∈ Icc 0 T, and the horizon truncation mildImageIcc (equal to the mild image on Icc 0 T, zero off it) discharges the xm1Section/viscousX1Section constructor obligations through mildImageIcc_aestronglyMeasurable / mildImageIcc_integrableXm1 / mildImageIcc_integrableX1. The unrestricted ∀t reading remains a FALSE premise (at t < 0 the multiplier exp(ν‖ξ‖²|t|) explodes while ha/ha1 give only polynomial control; structural witness a = exp(−‖ξ‖²) at ν = 1, t = −1); the restate WEAKENS the travelling premise, and the horizon is exactly what the completed slots read. CLOSED for general box elements: leaves 2a hmM and 2b hmXm1 at every horizon time (mildTimeLeaf_hmM / mildTimeLeaf_hmXm1), leaf 2f hmX1Int via the D₃ maximal-regularity package (mildTimeLeaf_hmX1Int), and leaf 2c X¹ in its maximal honest a.e.-time form (mildTimeLeaf_hmX1_ae); raw #print axioms over all 22 module declarations prints exactly [propext, Classical.choice, Quot.sound] (single-file compile exit 0 zero warnings plus locked audit build; ConditionalAudit emits the time-leaf receipts). CLOSED 2026-09-14 (lane L6d2): leaf 2d hmXm1Time for general quotient-represented boxes (mildTimeLeaf_hmXm1Time/_actualBox, strict) — the X⁻¹-weighted section is ContinuousOn the horizon (heat/Duhamel split, DCT dominated by the joint-weighted source; no s⁻¹ᐟ² smoothing needed at X⁻¹ weight), transferred via ContinuousOn.aestronglyMeasurable + measure monotonicity; salvage of the dead lane's continuity-bootstrap WIP, repaired to zero warnings. Inhabitation is settled the other way too: the zero-box row constructs a full instance. The a.e.-in-time fields (section measurabilities, polarization budgets) evade the pointwise objection: undischarged, not refuted"
    statement: "Construct MildAssemblyTimeLeaves for general NON-DEGENERATE box elements (the degenerate instance exists, see the zero-box row) — leaves 2a/2b/2f and the a.e.-time 2c are now CLOSED by the S3 theorems (evidence); the exact remaining time fields: (i) leaf 2c hmX1 at EVERY horizon time — D₃ maximal regularity supplies the Duhamel X¹ integrability only volume.restrict (Icc 0 T)-a.e. in t (mildTimeLeaf_hmX1_ae), and the ∀ t ∈ Icc 0 T upgrade needs the weighted source in L¹_t on every initial subinterval, which the box L²-based weights do not supply (s^{−1/2} ∈ L¹(0,T) \ L²(0,T)); (ii) leaf 2e hmX1Time — blocked at the SAME pointwise-Integrable proposition as (i); leaf 2d hmXm1Time was CLOSED 2026-09-14 by the X⁻¹-quotient continuity bootstrap (the mechanism names the asymmetry: the Duhamel difference is dominated by the joint-weighted source itself at X⁻¹ weight, no s⁻¹ᐟ² smoothing enters, whereas X¹ upgrades need exactly the failing L¹_t membership) — plus MildAssemblyPolarizationLeaves (budget decompositions 3a/3b): their packaging requires the all-ξ pointwise convolution-section integrabilities that mildLeafHjointL/R (a.e. joint measurability) does not supply, and the identities are not junk-stable on the null set where they fail. The separate named objects the record referenced — the pressure primitive and its physical inversion — landed 2026-09-13 and are consumed by the zero-box fixed-point module (see the P2 rows)."
  - id: "closed_mild_fixed_point_premise_witness"
    declaration: "Navier.Analysis.ContinuousLeiLinMildFixedPointPressure.existsUnique_mildFixedPoint_zeroBox"
    file: "Navier/Analysis/ContinuousLeiLinMildFixedPointPressure.lean"
    status: "THEOREM"
    axioms: ["propext", "Classical.choice", "Quot.sound"]
    evidence: "raw #print axioms 2026-09-13, lane P2 lock-free single-file compile exit 0 zero warnings zero sorryAx, all 17 public declarations strict; orchestrator to re-verify over the full audit build at landing; ConditionalAudit emits the record_zeroBox, zeroBox-fixed-point, pressureEq and conditional-feed receipts on every lake build"
    statement: "The MildAssemblyLeaves premise is WITNESSED INHABITED: record_zeroBox constructs a full instance at the zero datum (a = 0, R ≤ ν/16 at R = 0) — every field including the ∀t pointwise moment leaves, whose box slots are forced a.e.-zero through the RepresentativeGoodAt norm clause — and existsUnique_mildFixedPoint_zeroBox applies the conditional assembly to it, exhibiting a concrete unique mild fixed point of the actual mild self-map. Pressure consumption lands with it: fixedPoint_pressureEq (pressure of a fixed point's rep section = pressure of its own mild image, a.e.), continuousPressurePhysical_pairing_rep (the P1 physical pairing AT box representatives), zeroBox feed closure, and the conditional X⁻¹ gradient bound ≤ 4ν⁻¹R² with its integrability feeds named in the statement. Non-degenerate discharge remains the open obligation (instantiation row)."
  - id: "conditional_continuous_pressure_primitive"
    declaration: "Navier.Analysis.ContinuousLeiLinPressureReconstruction.continuousPressurePoisson_pairing"
    file: "Navier/Analysis/ContinuousLeiLinPressureReconstruction.lean"
    status: "CONDITIONAL_THEOREM"
    evidence: "raw #print axioms 2026-09-13 at this head, orchestrator-rerun in the fold-target tree: all 17 public declarations depend on axioms exactly [propext, Classical.choice, Quot.sound]; ConditionalAudit emits the Poisson-pairing, X¹-budget, X⁻¹-gradient and Duhamel-feed receipts on every lake build"
    statement: "Continuous-carrier pressure-reconstruction primitive: Riesz-symbol Fourier pressure p̂ = -Σᵢⱼ(ξᵢξⱼ/‖ξ‖²)(ûᵢ ⋆ ûⱼ), mean-zero normalized (p̂(0) = 0, no Dirac component), with ∫‖p̂‖ ≤ X⁰², normX1 p̂ ≤ 2·X⁰·X¹, the honest X⁻¹ statement on the gradient (the X⁻¹-mass-of-p claim is false in general by concentration at ξ = 0 and was replaced by ‖ξ‖⁻¹‖(ξᵢ)p̂‖), pointwise and Schwartz-test-function Poisson identities ‖ξ‖²·p̂ = -Σᵢⱼ ξᵢξⱼ·(uᵢ⋆uⱼ), and two Duhamel-shaped X⁻¹ feeds. Premises are the carrier's own measurability/integrability data. The physical-space inversion residual landed 2026-09-13 as a separate row (transported-pairing form); consumption by the mild fixed point landed the same day at the zero box and at box representatives (zero-box witness row)."
  - id: "conditional_physical_pressure_inversion"
    declaration: "Navier.Analysis.ContinuousLeiLinPressurePhysical.continuousPressurePhysical_pairing_physicalLaplacian"
    file: "Navier/Analysis/ContinuousLeiLinPressurePhysical.lean"
    status: "CONDITIONAL_THEOREM"
    evidence: "raw #print axioms 2026-09-13, orchestrator-rerun in the integration tree after lock build: all 14 public declarations depend on axioms exactly [propext, Classical.choice, Quot.sound]; ConditionalAudit emits the pairing, pointwise budget and inversion-bridge receipts on every lake build"
    statement: "Physical-space inversion of the pressure primitive: p = 𝓕⁻ p̂ with ‖p(x)‖ ≤ coordinateX0Mass u², continuity, zero-velocity truth check, and the distributional Poisson identity in transported-pairing form — ∫ p·Δψ = (2π)² ∫ (Σᵢⱼ ξᵢξⱼ·(ûᵢ⋆ûⱼ))·𝓕⁻ψ for Schwartz tests, under the same measurability/integrability carrier premises as the primitive. Named residual: rewriting the frequency side as the fully-physical ∫ (Σᵢⱼ ∂ᵢ∂ⱼ(vᵢvⱼ))·ψ needs the pointwise 𝓕⁻(ûᵢ⋆ûⱼ) = vᵢ·vⱼ product-to-convolution bridge for merely-L¹ frequency data (pinned Mathlib carries only the converse direction); closes under a Schwartz-u specialization. Fixed-point consumption landed 2026-09-13 (continuousPressurePhysical_pairing_rep at box representatives, fixedPoint_pressureEq at a fixed point — zero-box witness row)."
  - id: "conditional_critical_mild_mixed_terminal_bound"
    declaration: "Navier.Analysis.LeiLinCoerciveTerminal.CriticalMildMixedTerminalBound"
    file: "Navier/Analysis/LeiLinCoerciveTerminal.lean"
    status: "CONDITIONAL_THEOREM"
    evidence: "docs/OPEN_FRONTIER_MAP.md 2026-09-13 row 'Fourier-lattice continuation' (CONDITIONAL THEOREM)"
    statement: "CriticalMildMixedTerminalBound: a sufficient uniform nonlinear estimate to construct; its optimality is not established."
  - id: "conditional_continuous_lei_lin_dissipation"
    declaration: "Navier.Analysis.ContinuousLeiLinDissipation.integral_normX1_heatMode_le"
    file: "Navier/Analysis/ContinuousLeiLinDissipation.lean"
    status: "CONDITIONAL_THEOREM"
    evidence: "docs/OPEN_FRONTIER_MAP.md 2026-09-13 row 'Fourier-lattice continuation' (CONDITIONAL THEOREM): the proved whole-space linear transport half feeding the conditional continuation row"
    statement: "ContinuousLeiLinDissipation package: ν⁻¹ dissipation budget, no-spectral-gap free-term decay, coordinate-aggregated budgets, and Duhamel X⁻¹ bound with strict axioms."
