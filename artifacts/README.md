# Experiment artifacts

Only deterministic, replayable route-falsification artifacts belong here.
They are observation evidence, never theorem evidence.

The retained exact-output artifact records one deterministic finite scan. Its
driver lives in `experiments/`. Re-run the driver directly and compare its
output before relying on the observation. Neither the output nor a successful
replay is theorem evidence.
