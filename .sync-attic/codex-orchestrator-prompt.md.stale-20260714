# Codex orchestrator — Navier–Stokes Clay campaign, max-parallel attack

You are the ORCHESTRATOR (gpt-5.6-sol). Spawn one 5.6-sol lane per attack
avenue below, on DISJOINT fresh files, single-writer-per-file, fold + verify +
commit each lane on return. This is a 3D incompressible Navier–Stokes Clay
formalization campaign. Correct status beats optimistic status.

## Repo & state
- Path `~/navier` (git: https://github.com/awktavian/navier , branch
  `codex/navier-full-attack-20260714` — make your own disjoint sub-branch per lane
  if you spawn concurrently). Toolchain `leanprover/lean4:v4.31.0`, Mathlib `v4.31.0`.
- READ FIRST: `~/navier/AGENTS.md`, `~/navier/CONTRIBUTING.md`,
  `~/navier/docs/ATTACK.md`, `~/navier/docs/FORMALIZATION.md`,
  `~/navier/docs/BARRIERS.md`, `~/navier/data/attack_registry.json`.
- Already CLOSED this campaign (kernel-clean, do NOT redo): energy-supercritical
  obstruction (`Navier/EnergyObstruction.lean`: `energy_not_scale_coercive`,
  `l2_energy_dilation`, `l3_critical_dilation`, `lp_dilation_scaling`,
  `time_dilation_scaling`, `space_finrank`); rapid-decay convention bridge
  (`Navier/ConventionBridges.schwartzmap_satisfies_fefferman_rapid_decay`).
- The full analytic mixed-norm scaling `λ^{1-3/p-2/q}` is realized. Project status
  remains SCAFFOLDED / SCIENTIFIC_FRONTIER. `make check` is GREEN.

## SOUNDNESS FLOOR — hard bans (verbatim; do not relax)
- `StatementA` / `StatementC` are OPEN Clay Millennium problems. An artifact
  that CLAIMS either is proven must actually be certified by the kernel. NO
  `sorry`, NO `axiom` keyword, NO `True`/`PUnit`/`Unit`/trivial witness, NO
  wrapper that assumes its conclusion, NO result-as-hypothesis bridge, NO domain
  swap (2D/Euler/averaged/periodic presented as whole-space A), NO custom axiom.
- A closure claim requires: single-file `lake env lean <file>` GREEN + raw
  `#print axioms <decl>` ⊆ {propext, Classical.choice, Quot.sound, ± native_decide}.
- "Attempt maximally" is a VERB: pursue the math hard. "Prove" is a CLAIM: it
  needs the kernel to agree. A sorry-laundered / conditional-as-closure /
  True-typed artifact claiming `StatementA` is the one outcome worse than an
  honest failure. Attempt every route; claim EXACTLY what the kernel certifies
  (THEOREM / CONDITIONAL THEOREM / CONJECTURE / DECOMPOSED / RED).
- Repo commit-msg chain is installed. NEVER `--no-verify`. NEVER leave `>200`
  uncommitted lines on shared source.

## ATTACK AVENUES (one lane each; named file; mapped next-step given)
A. **Equation covariance** — `Navier/Analysis/Covariance.lean`. Prove
   `staticDivergence (fun y => c • u (c • y)) x = c^2 * staticDivergence u (c • x)`
   and that rescaling preserves incompressibility. MAPPED: `fderiv_comp` +
   `ContinuousLinearMap.fderiv` + `(fderiv ℝ u _).map_smul`. The raw-`fderiv`
   operators fight a CLM-coercion defeq (`⇑(c•id)` ≠ `HSMul.hSMul c`); DODGE it
   by carrying the dilation as a plain `Differentiable` function and using
   `hasFDerivAt.comp`, or by proving the per-component identity with `simp`
   carrying `[fderiv_comp, ContinuousLinearMap.fderiv, map_smul]`.
B. **Nonlinear energy conservation** — `Navier/Analysis/Energy.lean`. Prove
   `⟨(u·∇)u, u⟩ = 0` for divergence-free `u`. MAPPED: use the multivariable
   Schwartz-map IBP `SchwartzMap.integral_clm_comp_lineDerivOp_right_eq_neg_left`
   (and the 1-D `SchwartzMap.integral_mul_deriv_eq_neg_deriv_mul`). Rebundle the
   velocity as `SchwartzMap Space Space`; bridge the directional derivative
   `D_u u = (u·∇)u = ∑ⱼ uⱼ ∂ⱼu`; conclude via `div u = 0`.
C. **Vector-calculus identities** — `Navier/Analysis/VectorCalculus.lean`.
   Divergence product rule `div(f•u) = ∇f·u + f·div u`; div-of-gradient =
   Laplacian-trace; convection `convection` ↔ `(u·∇)u`. fderiv product rule
   (`DifferentiableAt.clm_smul` / `fderiv_smul`).
D. **R7 exact-symbol discriminator** — `Navier/Routes/R7/Discriminator.lean`.
   Set up a concrete divergence-free Fourier triad (`k1+k2+k3=0`, `a_i·k_i=0`)
   and search for an interaction/cancellation property of the EXACT NS bilinear
   form `P(u·∇)u` that is FALSE for Tao's averaged operator. THIS IS THE
   GENUINE PROBE AT THE MISSING IDEA. Likely DECOMPOSED/RED — if so, record the
   exact obstruction and the smallest statement that would survive Tao-averaging.
E. **Convention-bridge smoothness half** — extend `Navier/ConventionBridges.lean`.
   Bridge `ContDiff ℝ ⊤` ↔ `SchwartzMap.smooth'` (blocked only by an
   elaboration quirk; try unfolding the coercion / stating smoothness as
   `∀ n, ContDiff ℝ n f` and bridging via `ContDiff.of_le`).
F. **FeffermanC surface (optional, bookkeeping)** — `Navier/ClayC.lean`. A
   definitional `StatementC` matching Fefferman clauses (6),(7) WITH forcing
   (mirror `StatementA`; include periodic pressure if you also do B/D).

## INFRA / TOOLS — build and use
- lean4 LSP MCP (REPL-first): `lean_goal`, `lean_multi_attempt`, `lean_run_code`,
  `lean_loogle`, `lean_leansearch`, `lean_local_search`, `lean_state_search`,
  `lean_hammer_premise`. Run the search ladder before guessing lemma names.
- Kimina shared verifier: HTTP `:8765` (Studio) / `:8766` (Windows), MCP
  `kimina_check` / `kimina_check_file` / `kimina_check_batch` (project inferred
  from the absolute path). Fast candidate triage only — NOT closure authority.
- Verify: single-file `lake env lean <file>` (lock-free) + `#print axioms`; full
  `make check` (umbrella compile + axiom audit + `python3 -m unittest` + registry
  validate) before any wave-level claim. `make axioms` = `lake env lean Navier/AxiomAudit.lean`.
- If raw-`fderiv` operators keep fighting chain rules, BUILD the missing operator
  infrastructure on cleaner foundations (`hasFDerivAt`, `SchwartzMap`, the
  `EuclideanSpace`/`iteratedFDeriv` API) in a fresh `Navier/Analysis/` module.
- Edit `.lean` via Python `str.replace` (Edit/Write race the linter) or, for
  brand-new files, `Write`. One single-file `lake env lean` per edit.

## COMMIT CONTRACT (Codex)
- One commit per kernel-clean leaf. `git commit -o <exact-path>` (NEVER `-a`).
- `Co-authored-by: Codex` trailer (auto-stamped by the prepare-commit-msg hook
  when `KAGAMI_AGENT=cx`). Commit messages: WHAT/WHY/VERIFICATION; multi-file
  commits carry `staged-files: path/a path/b`.
- Closure-claim commits (closed/kernel-clean/GREEN/PROVED) MUST paste the raw
  `#print axioms` output in the body. Sense-check every claim against `make check`.

## RETURN CONTRACT (per lane, ≤20 lines)
VERDICT (CLOSED/DECOMPOSED/RED/FALSIFIED) · EVIDENCE (file + decl names + raw
`#print axioms` + commit SHA) · residual (strictly lower, named) · NEXT (one
concrete command or edit). A return with neither artifact nor checked witness is
REJECTED and re-dispatched narrower.

## ORCHESTRATION
- You are the orchestrator. Spawn 5.6-sol lanes (your native mechanism) one per
  avenue, DISJOINT files, fold each return: verify `lake env lean` + `#print
  axioms`, commit, refresh, refill the freed slot.
- MEASURED CAVEAT: Codex fan-out is token-heavy vs a shared-prefix Claude fleet
  (≤4.4 commits / 1M-out vs ~119). So PREFER FEWER, DEEPER lanes; pack 2–5
  sibling atoms per lane; do NOT spawn status-only or recon-only lanes. Route
  mechanical tactic-search to the cheapest capable tier.
- Keep a live in-flight roster (lane → file → expected artifact). End the run
  only when every avenue is CLOSED, DECOMPOSED-with-residual, or RED-with-witness.

## THE ONE HARD LINE
No fake `StatementA` / `StatementC` proof. Attempt every route at full effort;
certify exactly what the kernel proves; report RED/DECOMPOSED honestly when a
route hits a real obstruction (Tao's averaged-operator barrier, an
exact-structure gap, a Mathlib wall). The prize needs a new mathematical idea —
your job is to build all the infrastructure that idea will need and to run every
live route to its precise obstruction.
