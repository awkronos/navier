# Evolving-flow verification, 2026-09-09

Root independently compiled and materialized all three exact sources using
`~/.claude/hooks/lean-build-lock.sh 600 lake env lean -o <module.olean> <module.lean>`.
Every command exited 0. Source hashes are in `sources.json`.
Raw transitive axiom audits and compiler-resolved endpoint types are retained
in the adjacent files. The audited endpoints use only `propext`,
`Classical.choice`, and `Quot.sound`.

- `CriticalMildHigherMomentBootstrap`: the actual bounded local mild trajectory
  has raw five-eighth-generator moment and decoded spatial summability of
  order 2 + 1/4 at positive times. It does not construct all derivatives.
- `CriticalMildModeDifferentiation`: differentiates the actual mild equation
  coefficient by coefficient, including a separate right derivative at zero;
  its positive-time consumer proves the normalized unprojected physical
  Fourier coefficient equation with recovered pressure.
- `PeriodicGlobalCriticalControl`: an exact two-mode raw complex mild solution
  grows exponentially and refutes both unrestricted raw terminal bounds.
  The same file proves its initial datum violates physical anti-Hermitian
  reality. This counterexample does not refute physical periodic B.

The unresolved original endpoint still requires global physical control,
evolving reality, all-jet reconstruction, and the passage from the literal
Fourier convolution to the pointwise nonlinear PDE. Finite-grid Rust output
provides numerical experiments, not these continuum conclusions.
